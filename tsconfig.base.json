{
  "compilerOptions": {
    "target": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    // не преобразовывать JSX — позволяет поддерживать React, Vue, Solid и др.
    // сборщик сам конвертирует JSX в целевой runtime
    "jsx": "preserve",
    "baseUrl": "."
  }
}
